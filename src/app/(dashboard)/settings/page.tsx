import DashboardSettings from "@/components/dashboard/views/DashboardSettings"

export const metadata = {
  title: "Settings | Success Hub",
}

export default async function SettingsPage() {
  return <DashboardSettings />
}
